{
  "name": "nusuk",
  "version": "1.0.0",
  "description": "Nusuk: AI Elective Recommender (root workspace)",
  "private": true,
  "scripts": {
    "start": "npm --prefix server start",
    "dev": "npm --prefix server run dev",
    "install:all": "npm --prefix server install"
  }
}
